title: Agent Skills
description: AI coding agent skills (SKILL.md files) — works with hermes, claude code, codex, aider
theme: minima
markdown: kramdown
highlighter: rouge
permalink: /:basename/
defaults:
  - scope:
      path: ""
    values:
      layout: default
plugins:
  - jekyll-relative-links
